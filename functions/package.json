{
  "name": "daily-bake-functions",
  "private": true,
  "description": "Bakery — Cloud Functions: push notifications (FCM), Telegram relay, customer order updates",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  }
}
